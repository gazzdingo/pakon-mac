WRITE INTERLOCK -- engaged 2026-08-05, ahead of the PICkit 3 / ICSP session.

While this file exists, every tool in this directory that can WRITE, ERASE,
PROGRAM, or MODE-SWITCH the scanner refuses to run:

    flash_picm.py         erases + writes U11 application flash (I2C bootloader)
    eeprom_repair.py      writes the FX2 boot EEPROM (I2C 0x51)
    picm_run_app.py       --run / --enter mode-switch U11 and write its
                          internal EEPROM index 0 (the app-valid gate)
    pakon_cmd.py          can send arbitrary packets, including writes
    find_light_path.py    blind command sweeps (this is what mode-switched the
                          PICM into its bootloader in the first place)
    probe_sensor_path.py  writes A/D gain and offset registers
    init_ccd.py           writes FPGA and A/D registers
    start_acquire.py      writes the CCD control register
    lamp_on.py            writes light-board LED level/PWM registers
    test_extcode.py       loads and executes test code on the FX2 (the I2C
                          bus master)

Also quarantined: tools/i2c_eeprom.hex was renamed to
i2c_eeprom.hex.DANGEROUS-WRITES -- it contains a raw I2C EEPROM WRITE
operation and docs/13-eeprom-repair.md forbids using it.

The rule in force: READ EVERYTHING FIRST (docs/27-icsp-procedure.md).

Do not delete this file until ALL of the following are true:

  [ ] icsp_read_all.py --execute completed: five full-device reads of U11
      (bootloader 0x0000-0x03FF, full flash, config words, internal EEPROM)
  [ ] the tool reported all five reads byte-identical, per region, and the
      config words matched the expected values -- it REFUSES otherwise
  [ ] flash_diff.py issued a verdict (not WITHHELD) on the read
  [ ] the external I2C EEPROMs (0x50-0x57 range, incl. boot EEPROM 0x51 and
      the suspected calibration/PCS EEPROM at 0x52) were read five times with
      eeprom_backup.py and the reads agree
  [ ] every backup file is copied OFF this machine, twice

Deleting this file is the deliberate, human step that re-arms the write
tools. Even after that, flash_picm.py / eeprom_repair.py / picm_run_app.py
require a typed confirmation phrase before any non-volatile write.
